# 脚手架.cn — 建站与更新说明

## 一、这个站是什么

脚手架施工方案资讯站。纯静态站点，无需服务器、无需数据库，维护成本接近零。

| 项目 | 值 |
|---|---|
| 中文域名 | 脚手架.cn |
| punycode | `xn--kpuo4jd6z.cn` |
| 本地目录 | `C:\Users\Huawei\.workbuddy\2026-09-15-09-29-09\scaffold-cn-site` |
| 构建方式 | Python + markdown 库 → `public/`（可直接部署的静态文件） |
| 托管方式 | GitHub Pages（产物推 `gh-pages` 分支，源码推 `main`） |
| 发布方式 | `deploy.py` 走 GitHub REST API（不走 git push） |
| 仓库 | `https://github.com/mfujun2025/jiaoshoujia`（public） |
| 站点地址 | `https://xn--kpuo4jd6z.cn/`（DNS 配好后生效） |

## 二、页面结构

```
/                          首页（分类导航 + 重点推荐 + 最新资讯）
/news/                     全部方案资讯，按日期倒序分组，分页每页 12 篇
/news/<slug>/              文章详情页（含自动目录、关键词、相关阅读）
/category/rule/            规范与政策
/category/scheme/          专项方案
/category/type/            架体类型
/category/safe/            安全与验收
/category/case/            事故案例
/search/                   站内搜索（前端索引，支持高亮与摘要定位）
/about/                    关于本站与更新方式
/sitemap.xml  /robots.txt  搜索引擎用的文件
```

响应式适配：桌面（≥1180px 五列分类卡、三列文章卡）、平板、手机（单列 + 汉堡菜单）。

## 三、目录结构

```
scaffold-cn-site/
├── content/              ← 文章源文件，一篇一个 .md
├── templates/            页面模板（{{TOKEN}} 占位）
├── static/               样式与脚本（会整体复制到 public/）
├── public/               构建产物，不要手工改
├── build.py              构建脚本
├── check.py              产物自检（内链、索引、占位符）
├── deploy.py             发布脚本
├── site.json             站点配置（名称、导航、描述）
└── categories.json       分类定义
```

## 四、本机预览

```bash
PY="C:/Users/Huawei/.workbuddy/binaries/python/envs/default/Scripts/python.exe"
cd "C:/Users/Huawei/.workbuddy/2026-09-15-09-29-09/scaffold-cn-site"

"$PY" build.py                    # 构建
"$PY" check.py                    # 自检
"$PY" -m http.server 8792 --directory public   # 起预览
# 浏览器打开 http://127.0.0.1:8792/
```

> **注意端口**：8791 端口上常驻着另一个会话遗留的服务进程，直接用 8792 或 8793，避免请求被别的服务接走。

## 五、新增一篇资讯（日常更新就这一件事）

1. 在 `content/` 下新建一个 `.md` 文件，文件名即 URL 里的 slug。

2. 头部填 front matter，八个字段别少：

```yaml
---
title: 标题（含核心关键词，30 字以内）
slug: english-hyphen-slug
category: rule          # rule / scheme / type / safe / case 五选一
date: 2026-09-20
featured: true          # 可选，true 会进首页「重点推荐」
desc: 60~90 字，说清解决什么问题。会用在列表页、卡片和页面的 meta description
keywords: 关键词1,关键词2,关键词3,关键词4
---
```

3. 写正文。建议 1500~2500 字，4~6 个二级标题用中文序号（`## 一、xxx`），至少放一个表格。

4. 构建 + 自检：

```bash
"$PY" build.py && "$PY" check.py
```

5. 发布：

```bash
"$PY" deploy.py --source     # 产物 → gh-pages，源码 → main
```

**新增文章不需要改任何模板、列表页或 sitemap**——`build.py` 会自动重建首页卡片、列表页、分类页、搜索索引和 sitemap。

## 六、内容写作的三条硬规矩

1. **数值必须准。** 规范编号、判定阈值、系数，不确定就联网核实，宁可不写也不要写错。这是本站最不能出的错。
2. **标准编号要核实现行版本。** 例如盘扣式现行的是 JGJ/T 231-2021（不是 JGJ 231-2010）；脚手架顶层依据是 GB 55023-2022。
3. **不编造案例。** 事故类内容只做公开调查结论的共性归纳，不虚构具体事故、时间、伤亡数字。

## 七、上线状态：GitHub 侧已完成，只剩改 DNS

**已完成（脚本 + API 自动处理）：**

| 项目 | 状态 |
|---|---|
| 仓库 `mfujun2025/jiaoshoujia` | ✅ 已存在（public，默认分支 main） |
| 站点产物推送 `gh-pages` | ✅ 24 个文件 |
| 源码推送 `main` | ✅ 22 个文件 |
| GitHub Pages 启用（源 = gh-pages / root） | ✅ 已启用，构建状态 built |
| 自定义域名 `xn--kpuo4jd6z.cn` | ✅ 已在仓库 Pages 设置里绑定 |
| `CNAME` 文件 | ✅ 由 `build.py` 产出并发布 |
| `.nojekyll` | ✅ 已发布（关闭 Jekyll 处理） |
| 线上可访问性自检 | ✅ 10 个路径全部 200 |

**唯一待办：到爱名网配 DNS。**

### DNS 怎么配（关键，别配错）

`脚手架.cn` 是**裸域名（apex domain）**，没有 `www` 前缀。裸域名**不能用 CNAME 记录**——国内 DNS 服务商对 apex 域的 CNAME 支持普遍不完整，配了大概率不生效。

**正确做法：在爱名网的域名解析控制台，添加 4 条 A 记录：**

| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| `@` | A | `185.199.108.153` |
| `@` | A | `185.199.109.153` |
| `@` | A | `185.199.110.153` |
| `@` | A | `185.199.111.153` |

> 这 4 个 IP 是 GitHub Pages 官方地址，已在官方文档核实。
>
> 如果爱名网的解析界面支持 **ALIAS** 或 **ANAME** 记录类型，也可以只加一条 `@ → mfujun2025.github.io`，效果等同。

**可选：IPv6 支持**（非必需，但加了更好）。再添加 4 条 AAAA 记录，值分别为 `2606:50c0:8000::153`、`2606:50c0:8001::153`、`2606:50c0:8002::153`、`2606:50c0:8003::153`。

**配完等生效：**

DNS 一般几分钟到几小时生效。生效后 GitHub 会自动为域名签发 HTTPS 证书（约 15 分钟到 24 小时）。

验证方法：

```bash
nslookup -type=A xn--kpuo4jd6z.cn 8.8.8.8
# 结果应显示上面那 4 个 185.199.x.153
```

证书签发后，在仓库 Settings → Pages 里把 **Enforce HTTPS** 勾上（证书没签发前这个选项是灰的，等它亮）。

> **关于中文域名**：GitHub Pages 的 Custom domain 一律填 punycode 形式 `xn--kpuo4jd6z.cn`，填中文会报错。浏览器里中文域名和 punycode 都能访问，不用管。

## 八、域名当前状态（已核实）

```
nslookup xn--kpuo4jd6z.cn  →  45.249.246.124（爱名网停放页）
```

说明域名已注册但 DNS 尚未指向 GitHub。**在 A 记录配好之前，这个域名访问到的是注册商的停放广告页**，不是本站——别以为站点没生效。

另：自定义域名一旦绑定，`https://mfujun2025.github.io/jiaoshoujia/` 会 301 跳转到 `https://xn--kpuo4jd6z.cn/`。所以 DNS 配好前，两个地址都打不开本站，这是正常的。

## 九、已知限制

- **不做服务端功能。** 搜索是前端索引（`search-index.json`），文章量到几百篇之前性能没问题；再多可以考虑拆分索引。
- **没有评论、没有后台。** 内容更新靠改 `content/*.md` 后重新构建发布，这也是它维护成本低的原因。
- **`public/` 会被构建脚本全量重写**，不要往里手工放文件。需要随站发布的静态文件放 `static/`（构建时整体递归复制）。
- **构建脚本不清空 `public/`**（本机 Python 删除操作被路由到系统回收站，`rmtree` 会失败），采用覆盖式写入。因此删掉一篇文章后，旧的详情页 HTML 会残留在 `public/` 里。**如果删过文章，删完手动清理对应的 `public/news/<slug>/` 目录再发布。**
- **`CNAME` 必须由 `build.py` 产出，不要指望 GitHub 后台生成。** `deploy.py` 推送 `gh-pages` 时是不带 `base_tree` 的覆盖式推送，会清掉分支上 GitHub 自动写入的 `CNAME` 文件，导致自定义域名被解绑、站点掉线。所以构建脚本里固定输出 `CNAME` 和 `.nojekyll`，这也是 `deploy.py` 里唯一放行的隐藏文件。
- **GitHub Pages 在国内访问不稳定。** 时通时不通是常态，主要面向中文用户的话，后续可考虑套一层国内可用的 CDN，或迁移到其他托管。目前先保证站点本身没问题。

## 十、这个站的规范依据（内容可信度说明）

站内内容的规范依据以住建部及全国标准信息公共服务平台公布的现行版本为准，核心包括：

| 编号 | 名称 |
|---|---|
| GB 55023-2022 | 施工脚手架通用规范（全文强制，2022-10-01 实施） |
| GB 51210-2016 | 建筑施工脚手架安全技术统一标准 |
| JGJ 130-2011 | 建筑施工扣件式钢管脚手架安全技术规范 |
| JGJ/T 231-2021 | 建筑施工承插型盘扣式钢管脚手架安全技术标准 |
| JGJ 166-2016 | 建筑施工碗扣式钢管脚手架安全技术规范 |
| JGJ 202-2010 | 建筑施工工具式脚手架安全技术规范 |
| 住建部令第 37 号 | 危险性较大的分部分项工程安全管理规定 |

---

*本文档随站点一起维护。改了构建脚本或目录结构，记得回来同步这一份。*
