# 脚手架.cn — 建站与更新说明

## 一、这个站是什么

脚手架施工方案资讯站。纯静态站点，无需服务器、无需数据库，维护成本接近零。

| 项目 | 值 |
|---|---|
| 中文域名 | 脚手架.cn |
| punycode | `xn--kpuo4jd6z.cn` |
| 本地目录 | `C:\Users\Huawei\.workbuddy\2026-09-15-09-29-09\scaffold-cn-site` |
| 构建方式 | Python + markdown 库 → `public/`（可直接部署的静态文件） |
| 托管方式 | GitHub Pages（产物推 `gh-pages` 分支，源码推 `main`） |
| 发布方式 | `deploy.py` 走 GitHub REST API（不走 git push） |
| 仓库 | `https://github.com/mfujun2025/jiaoshoujia`（public） |
| 站点地址 | **https://xn--kpuo4jd6z.cn/**（已上线） |

## 二、页面结构

```
/                          首页（分类导航 + 重点推荐 + 最新资讯）
/standards/                规范标准资料库（政府公开文件下载 + 适用范围 + 来源与版权说明）
/standards/<文件>.pdf      可下载的强制性规范与政策法规文件（ASCII 文件名）
/standards/<文件>.xlsx     规范标准清单 Excel 台账
/news/                     全部方案资讯，按日期倒序分组，分页每页 12 篇
/news/<slug>/              文章详情页（含自动目录、关键词、相关阅读）
/category/rule/            规范与政策
/category/scheme/          专项方案
/category/type/            架体类型
/category/safe/            安全与验收
/category/case/            事故案例
/search/                   站内搜索（前端索引，支持高亮与摘要定位）
/about/                    关于本站与更新方式
/sitemap/                  网站地图（HTML 版，给访客看）
/sitemap.xml  /robots.txt  搜索引擎用的文件
```

响应式适配：桌面（≥1180px 五列分类卡、三列文章卡）、平板、手机（单列 + 汉堡菜单）。

## 三、目录结构

```
scaffold-cn-site/
├── content/              ← 文章源文件，一篇一个 .md
├── templates/            页面模板（{{TOKEN}} 占位）
├── static/               样式与脚本（会整体复制到 public/）
│   └── standards/        ← 资料库可下载文件（PDF / XLSX），文件名用 ASCII
├── public/               构建产物，不要手工改
├── build.py              构建脚本
├── check.py              产物自检（内链、索引、占位符、资料库文件完整性）
├── deploy.py             发布脚本
├── site.json             站点配置（名称、导航、描述）
├── categories.json       分类定义
└── standards.json        资料库数据（分组 / 文号 / 来源 / 格式说明）
```

### 资料库怎么加一份文件

1. 把文件改名为 **ASCII 短名**（如 `JBZ-2021-48.pdf`）放进 `static/standards/`；
2. 在 `standards.json` 对应分组的 `items` 里加一条（`code / name / file / format / fmt_note / source / note / tag`）；
3. `python build.py && python check.py` —— 文件缺失构建会直接报错，大小由脚本现场统计，不用手填。

> **不要往 `public/standards/` 手工放文件**，构建不会清空 `public/`，但下次 `deploy.py` 只推 `build.py` 产生的清单，手工文件会漏推或残留成孤儿。
> **行业标准（JGJ / JGJ/T）正文有版权，不能放**；只有国家机关的强制性规范、部门规章、部发文件可以依法转载。

## 四、本机预览

```bash
PY="C:/Users/Huawei/.workbuddy/binaries/python/envs/default/Scripts/python.exe"
cd "C:/Users/Huawei/.workbuddy/2026-09-15-09-29-09/scaffold-cn-site"

"$PY" build.py                    # 构建
"$PY" check.py                    # 自检
"$PY" -m http.server 8792 --directory public   # 起预览
# 浏览器打开 http://127.0.0.1:8792/
```

> **注意端口**：8791 端口上常驻着另一个会话遗留的服务进程，直接用 8792 或 8793，避免请求被别的服务接走。

## 五、新增一篇资讯（日常更新就这一件事）

1. 在 `content/` 下新建一个 `.md` 文件，文件名即 URL 里的 slug。

2. 头部填 front matter，八个字段别少：

```yaml
---
title: 标题（含核心关键词，30 字以内）
slug: english-hyphen-slug
category: rule          # rule / scheme / type / safe / case 五选一
date: 2026-09-20
featured: true          # 可选，true 会进首页「重点推荐」
desc: 60~90 字，说清解决什么问题。会用在列表页、卡片和页面的 meta description
keywords: 关键词1,关键词2,关键词3,关键词4
---
```

3. 写正文。建议 1500~2500 字，4~6 个二级标题用中文序号（`## 一、xxx`），至少放一个表格。

4. 构建 + 自检：

```bash
"$PY" build.py && "$PY" check.py
```

5. 发布：

```bash
"$PY" deploy.py --source     # 产物 → gh-pages，源码 → main
```

**新增文章不需要改任何模板、列表页或 sitemap**——`build.py` 会自动重建首页卡片、列表页、分类页、搜索索引和 sitemap。

## 六、内容写作的三条硬规矩

1. **数值必须准。** 规范编号、判定阈值、系数，不确定就联网核实，宁可不写也不要写错。这是本站最不能出的错。
2. **标准编号要核实现行版本。** 例如盘扣式现行的是 JGJ/T 231-2021（不是 JGJ 231-2010）；脚手架顶层依据是 GB 55023-2022。
3. **不编造案例。** 事故类内容只做公开调查结论的共性归纳，不虚构具体事故、时间、伤亡数字。

## 七、上线状态：已上线 ✅

**站点已通过自定义域名正常对外服务，全站 14 个路径实测返回 200。**

访问入口：**https://xn--kpuo4jd6z.cn/**

| 项目 | 状态 |
|---|---|
| 仓库 `mfujun2025/jiaoshoujia` | ✅ public，默认分支 main |
| 站点产物 → `gh-pages` | ✅ 25 个文件 |
| 源码 → `main` | ✅ 23 个文件 |
| GitHub Pages | ✅ 已启用（源 = gh-pages / root） |
| 自定义域名 | ✅ 已绑定 `xn--kpuo4jd6z.cn` |
| `CNAME` / `.nojekyll` | ✅ 由 `build.py` 产出并发布 |
| DNS 解析 | ✅ 已生效 |
| HTTPS | ✅ 正常，Let's Encrypt 证书 |
| 全站自检 | ✅ 所有路径 200，404 正确返回 |

### 实际走的架构：Cloudflare 代理 + GitHub Pages

实测 DNS 解析到的是 **Cloudflare** 的 IP（`172.67.209.29` / `104.21.69.141`），不是 GitHub 的 `185.199.x.153`。当前链路：

```
访客 → Cloudflare 边缘节点 → GitHub Pages（mfujun2025.github.io）
```

这不是问题，反而有好处（全球节点、基础防护、省 GitHub 的带宽）。但要知道三件事：

1. **`github.io` 地址现在会 301 跳转到自定义域名** —— 这是绑定 custom domain 后的正常行为，不是故障。
2. **Cloudflare 的 SSL 模式必须是 Full 或 Full (Strict)，不能是 Flexible。** 否则 CF 用 http 回源，一旦 GitHub 侧又开了 Enforce HTTPS，就会形成无限重定向循环。
3. **要强制 HTTPS，在 Cloudflare 开 "Always Use HTTPS"**，不要动 GitHub Pages 的 Enforce HTTPS（在 CF 代理下容易踩上面那个循环）。目前两边都没开，站点 http/https 都能访问，功能正常。

### robots.txt 被 Cloudflare 改写了（要知道）

实测线上 `/robots.txt` 的**前 40 行**是 Cloudflare 自动注入的 "Managed robots.txt / Content Signals"，我们自己的声明被挤到最后：

```
# BEGIN Cloudflare Managed content
User-agent: *
Content-Signal: search=yes,ai-train=no,use=reference
...
User-agent: GPTBot
Disallow: /
User-agent: ClaudeBot
Disallow: /
User-agent: Bytespider
Disallow: /
# END Cloudflare Managed Content

User-agent: *           ← 这里才是我们的
Allow: /
Sitemap: https://xn--kpuo4jd6z.cn/sitemap.xml
```

影响：

- **不影响百度、Google 收录** —— 通用 `User-agent: *` 是 `Allow: /`，Sitemap 声明也还在。
- **但禁掉了主流 AI 爬虫**（GPTBot / ClaudeBot / Bytespider / CCBot 等）。如果希望内容被 AI 搜索抓取和引用（豆包、Kimi、Perplexity 之类），需要到 Cloudflare 控制台关掉这个托管的 robots.txt 功能。

这是 Cloudflare 的默认行为，不是本站配置问题。

### 若要在别处重新部署（DNS 参考）

`脚手架.cn` 是**裸域名（apex domain）**，没有 `www` 前缀。裸域名**不能用 CNAME 记录**——国内 DNS 服务商对 apex 域的 CNAME 支持普遍不完整，配了大概率不生效。

GitHub Pages 官方推荐的 A 记录（4 条）：

| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| `@` | A | `185.199.108.153` |
| `@` | A | `185.199.109.153` |
| `@` | A | `185.199.110.153` |
| `@` | A | `185.199.111.153` |

可选 IPv6（AAAA）：`2606:50c0:8000::153`、`2606:50c0:8001::153`、`2606:50c0:8002::153`、`2606:50c0:8003::153`。

若服务商支持 **ALIAS / ANAME**，也可只加一条 `@ → mfujun2025.github.io`。CNAME 只适用于**子域名**（如 `www.脚手架.cn`）。

> **中文域名**：GitHub Pages 的 Custom domain 一律填 punycode `xn--kpuo4jd6z.cn`，填中文会报错。浏览器里中文域名和 punycode 都能访问。

## 八、网站地图

站点有两份地图，都由 `build.py` 自动生成。**新增文章后无需手工维护**——重建时 XML 自动多一条，HTML 版自动归入对应分类。

| 版本 | 地址 | 用途 |
|---|---|---|
| HTML 版 | `/sitemap/` | 给访客看：一页列出全部页面与文章，按分类分组、带日期。页脚「网站地图」入口指向它 |
| XML 版 | `/sitemap.xml` | 给搜索引擎看：含 `lastmod` 与 `priority`，已由 `robots.txt` 声明 |

当前规模：XML 16 条 URL，HTML 版 16 条索引。

**提交给搜索引擎**（域名已生效，现在就能做）：

- **百度**：百度搜索资源平台 → 添加站点 `xn--kpuo4jd6z.cn` → 验证归属 → 提交 `https://xn--kpuo4jd6z.cn/sitemap.xml`
- **必应**：Bing Webmaster Tools，同上
- **Google**：Google Search Console，同上

> 站点开了 Cloudflare 代理，搜索引擎的验证方式（HTML 文件 / meta 标签）都能正常通过；如果验证失败，检查 CF 是否缓存了 `robots.txt` 或验证文件。

### 归属验证文件放哪里（重要）

搜索引擎会让你下载一个验证文件，要求放到**网站根目录**。本站没有服务器，放法固定为：

**把验证文件丢进 `static/` 目录 → 重新构建 → 发布。** `build.py` 会把 `static/` 里的所有文件**平铺复制到站点根目录**，所以：

```
static/BingSiteAuth.xml   →   https://xn--kpuo4jd6z.cn/BingSiteAuth.xml
static/baidu_verify_xxx.html → https://xn--kpuo4jd6z.cn/baidu_verify_xxx.html
```

**不要**手工往 `public/` 里放文件——`public/` 是构建产物，每次重建都会覆盖，但你手工放的文件不会随 `deploy.py` 的清单走（推是可以推，但下次重建容易乱）。**唯一正确入口是 `static/`。**

常见文件名（各家格式不同，以平台实际给出的为准）：

| 平台 | 文件名形态 | 内容 |
|---|---|---|
| 必应 | `BingSiteAuth.xml` | `<users><user>32位大写十六进制码</user></users>` |
| 百度 | `baidu_verify_codeva-XXXXXXXX.html` | 一行指定字符串 |
| Google | `googleXXXXXXXXXXXXXXXX.html` | 一行 `google-site-verification: ...` |

> **关于必应的验证码是否可以在多个站之间复用**：必应官方文档的表述含糊——既说"为你的**这个属性**生成唯一文件"，又说该码"与你的**账户**关联"，还提示"每个要单独监控的属性重复此流程"。网上资料也两种说法都有。**本站的实测证据是：`脚手架.cn` 用的码（`8CD269ED...`）与 `密封件.cn` 的完全相同**，说明同一个必应账号下很可能是**账号级验证码**，跨站复用能通过。但这不保证，**最终以必应后台点「验证」的结果为准**。若不通过，改用下文「meta 标签」方式（最稳，不依赖根目录文件）。

### 必应验证：当前实测状态（2026-09-15）

`BingSiteAuth.xml` 已发布到站点根目录，逐字节与必应文件一致。所有前置条件实测通过：

| 检查项 | 结果 |
|---|---|
| `https://xn--kpuo4jd6z.cn/BingSiteAuth.xml` | **200**，`Content-Type: application/xml`，85 字节，MD5 一致 |
| XML 合法性 | 根节点 `users`，`<user>` 值可正确解析 |
| robots.txt 是否禁 bingbot | **没有**（CF 托管段禁的是 GPTBot/ClaudeBot/Bytespider/CCBot 等 AI 爬虫，通用 UA 仍 `Allow: /`） |
| bingbot UA 实测抓取 | **200**（文件和首页都能抓到） |
| http 访问 | 直接 200，**不重定向**（必应要求最终 URL 仍返回文件内容） |
| www 版本 | 301 → 裸域名（正常）。**必应里要提交裸域名 `https://xn--kpuo4jd6z.cn`**——Bing 视 www 和非 www 为两个不同属性 |
| CF 缓存头 | `cf-cache-status: DYNAMIC`，无异常 |

**剩下的动作（只能你去点）：** 在必应后台该站点的验证页点「Verify」。通过 → 接着提交 sitemap；不通过 → 把结果告诉我，改用 meta 标签方式（加在 `templates/layout.html` 的 `<head>`，我改一次全站生效）。

## 九、已知限制

- **不做服务端功能。** 搜索是前端索引（`search-index.json`），文章量到几百篇之前性能没问题；再多可以考虑拆分索引。
- **没有评论、没有后台。** 内容更新靠改 `content/*.md` 后重新构建发布，这也是它维护成本低的原因。
- **`public/` 会被构建脚本全量重写**，不要往里手工放文件。需要随站发布的静态文件放 `static/`（构建时整体递归复制）。
- **构建脚本不清空 `public/`**（本机 Python 删除操作被路由到系统回收站，`rmtree` 会失败），采用覆盖式写入。因此删掉一篇文章后，旧的详情页 HTML 会残留在 `public/` 里。**如果删过文章，删完手动清理对应的 `public/news/<slug>/` 目录再发布。**
- **`CNAME` 必须由 `build.py` 产出，不要指望 GitHub 后台生成。** `deploy.py` 推送 `gh-pages` 时是不带 `base_tree` 的覆盖式推送，会清掉分支上 GitHub 自动写入的 `CNAME` 文件，导致自定义域名被解绑、站点掉线。所以构建脚本里固定输出 `CNAME` 和 `.nojekyll`，这也是 `deploy.py` 里唯一放行的隐藏文件。
- **Cloudflare 会缓存静态资源。** HTML 的响应头是 `cf-cache-status: DYNAMIC`（不缓存，所以更新内容立即生效），但 `.css` / `.js` 这类可能被边缘节点缓存。**改了样式却发现线上没变，先到 CF 控制台「缓存 → 清除缓存」**，再怀疑代码。
- **国内访问质量取决于 Cloudflare 节点。** 已套 CF 代理，比裸连 GitHub Pages 稳定，但高峰期仍可能有波动。若后续要求更高，可考虑 CF 的国内加速方案或换托管。
- **上线后建议尽快提交搜索引擎**，否则收录靠自然抓取会比较慢。做法见第八节。

## 十、这个站的规范依据（内容可信度说明）

站内内容的规范依据以住建部及全国标准信息公共服务平台公布的现行版本为准，核心包括：

| 编号 | 名称 |
|---|---|
| GB 55023-2022 | 施工脚手架通用规范（全文强制，2022-10-01 实施） |
| GB 51210-2016 | 建筑施工脚手架安全技术统一标准 |
| JGJ 130-2011 | 建筑施工扣件式钢管脚手架安全技术规范 |
| JGJ/T 231-2021 | 建筑施工承插型盘扣式钢管脚手架安全技术标准 |
| JGJ 166-2016 | 建筑施工碗扣式钢管脚手架安全技术规范 |
| JGJ 202-2010 | 建筑施工工具式脚手架安全技术规范 |
| 住建部令第 37 号 | 危险性较大的分部分项工程安全管理规定 |

---

*本文档随站点一起维护。改了构建脚本或目录结构，记得回来同步这一份。*
